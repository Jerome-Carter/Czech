{% if student.state %}
<ul id="user1" class="dropdown-content">
  <li><a href="{{ path_for('settings') }}" class="blue-grey-text"><i class="black-text material-icons left">settings</i>Settings</a></li>
  <li class="divider"></li>  <li><a href="{{ path_for('logout') }}" class="blue-grey-text"><i class="black-text material-icons left">exit_to_app</i>Logout</a></li>
</ul>
<ul id="user2" class="dropdown-content">
  <li><a href="{{ path_for('settings') }}" class="blue-grey-text"><i class="black-text material-icons left">settings</i>Settings</a></li>
  <li class="divider"></li>
  <li><a href="{{ path_for('logout') }}" class="blue-grey-text"><i class="black-text material-icons left">exit_to_app</i>Logout</a></li>
</ul>
{% endif %}
<nav class="white" role="navigation">
  <div class="nav-wrapper container">
    <a id="logo-container" href="#" class="brand-logo">Intern Check In / Out</a>
    <ul class="right hide-on-med-and-down">
      <li><a href="{{ path_for('main') }}"><i class="material-icons left">home</i>Home</a></li>
      {% if student.state and student.rank %}
      <li><a href="{{ path_for('manageInterns') }}"><i class="material-icons left">people</i>Manage Interns</a></li>
      <li><a href="{{ path_for('log') }}"><i class="material-icons left">description</i>View Timesheet</a></li>
      <li><a class="dropdown-button" data-activates="user1"><i class="material-icons left">account_circle</i>{{ student.data.first_name }} {{ student.data.last_name }}<i class="material-icons right">arrow_drop_down</i></a></li>
      {% elseif student.state %}
      <li><a class="dropdown-button" data-activates="user1"><i class="material-icons left">account_circle</i>{{ student.data.first_name }} {{ student.data.last_name }}<i class="material-icons right">arrow_drop_down</i></a></li>
      {% else %}
      <li><a href="{{ path_for('login') }}"><i class="material-icons left">vpn_key</i>Login</a></li>
      {% endif %}
    </ul>

    <ul id="nav-mobile" class="side-nav">
      <li><a href="{{ path_for('main') }}"><i class="material-icons left">home</i>Home</a></li>
      {% if student.state and student.rank %}
      <li><a href="{{ path_for('manageInterns') }}"><i class="material-icons left">people</i>Manage Interns</a></li>
      <li><a href="{{ path_for('log') }}"><i class="material-icons left">description</i>View Timesheet</a></li>
      <li><a href="{{ path_for('settings') }}"><i class="black-text material-icons left">settings</i>Settings</a></li>
      <li><a href="{{ path_for('logout') }}"><i class="black-text material-icons left">exit_to_app</i>Logout</a></li>
      {% elseif student.state %}
      <li><a href="{{ path_for('settings') }}"><i class="black-text material-icons left">settings</i>Settings</a></li>
      <li><a href="{{ path_for('logout') }}"><i class="black-text material-icons left">exit_to_app</i>Logout</a></li>
      {% else %}
      <li><a href="{{ path_for('login') }}"><i class="material-icons left">vpn_key</i>Login</a></li>
      {% endif %}
    </ul>
    <a href="#" data-activates="nav-mobile" class="button-collapse"><i class="blue-grey-text material-icons">menu</i></a>
  </div>
</nav>